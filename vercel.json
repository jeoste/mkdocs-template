{
  "buildCommand": false,
  "outputDirectory": "public",
  "devCommand": "mkdocs serve",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "python3 -m pip install --upgrade pip && pip install -r requirements.txt && mkdocs build -d public",
        "outputDirectory": "public"
      }
    }
  ]
} 